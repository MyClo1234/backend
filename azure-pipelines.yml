trigger:
- main
- dev

pool:
  name: 'codify-self-host-ci-cd'

steps:
  - task: ArchiveFiles@2
    displayName: "Archive files"
    inputs:
      rootFolderOrFile: '$(System.DefaultWorkingDirectory)'
      includeRootFolder: false
      archiveType: 'zip'
      archiveFile: '$(System.DefaultWorkingDirectory)/build.zip'
      replaceExistingArchive: true

  - script: |
      # 리눅스 환경에서 가장 확률 높은 정석 엔드포인트입니다.
      DEPLOY_URL="https://codify-functions-backend-gzaydqgch0ccbdfe.scm.koreacentral-01.azurewebsites.net/api/zipdeploy"
      
      echo "Final attempt to: $DEPLOY_URL"
      
      # 인증은 이미 뚫렸으니 주소만 맞으면 202 Accepted가 뜰 겁니다.
      curl -f -v -u "${USER}:${PASS}" \
      -H "Content-Type: application/zip" \
      --data-binary @"$(System.DefaultWorkingDirectory)/build.zip" \
      "$DEPLOY_URL"
    displayName: "Deploy to Azure Functions (Final Path Try)"
    env:
      USER: '$(DEPLOY_USER)'
      PASS: '$(DEPLOY_PWD)'